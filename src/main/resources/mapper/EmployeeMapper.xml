<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apan.libary.dao.EmployeeMapper">
    <!-- public Employee getEmpl(Integer id);-->
    <select id="getEmpl" resultType="com.apan.libary.bean.Employee">
        select * from emplee where id=#{id}
    </select>
</mapper>
